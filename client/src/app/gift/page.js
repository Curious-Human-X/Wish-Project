'use client';

import MemoryPath from '../../components/MemoryPath';

export default function GiftPage() {
  return (
    <main className="min-h-screen w-full bg-stone-100 pb-20">
      {/* The entire experience is now handled inside this component */}
      <MemoryPath />
    </main>
  );
}